{
	"_comment_purpose": "High-level v2 workflow definition. This file intentionally does NOT contain model ids, tool names, prompt text, or reviewer-swarm configuration. It only references catalog entries by id. Brain orchestration is constrained to meta.activeAgents.",
	"_comment_catalog_files": "All concrete model ids, tool lists, prompt packs, and quality gates live in catalog files. v2 runtime consumes these catalog references via the resolved workflow graph.",
	"_comment_reviewer_swarm": "Reviewer-swarm goals live in the quality-gates catalog and are attached to the reviewer agent via `agentCatalog.agents[reviewer-default].qualityGates`; the resolver derives the swarm identity from resolved reviewer quality gates whose catalog gate has `kind: review-goal`.",
	"version": 2,
	"meta": {
		"name": "default-pi-workflow",
		"description": "Default three-agent flow: brain orchestrates, coder implements, reviewer reviews with a goal-aligned reviewer swarm.",
		"activeAgents": ["brain", "coder", "reviewer"]
	},
	"direction": "sequential",
	"flow": [
		{ "role": "brain" },
		{ "role": "coder" },
		{ "role": "reviewer" }
	],
	"roles": [
		{ "role": "brain", "agent": "brain-default" },
		{ "role": "coder", "agent": "coder-default" },
		{ "role": "reviewer", "agent": "reviewer-default" }
	],
	"references": {
		"agentCatalog": "workflow.agent-catalog.json",
		"modelPresets": "workflow.model-presets.json",
		"toolProfiles": "workflow.tool-profiles.json",
		"promptPacks": "workflow.prompt-packs.json",
		"qualityGates": "workflow.quality-gates.json"
	},
	"deepPlanning": {
		"enabled": false,
		"plannerCount": 2,
		"maxConcurrency": 2,
		"rounds": 2,
		"roomIdPrefix": "deep-plan",
		"planners": [
			{
				"id": "pr-agent-1",
				"role": "product-requirements",
				"modelPreset": "premium-planner",
				"thinkingLevel": "xhigh",
				"instructions": "You are a Product Requirements agent. In a bounded grill-me discussion with one other PR agent, ask at most one highest-value question per round with a recommended answer. Inspect the codebase before asking when possible. Update the shared PRD with resolved decisions and open questions. Do not produce implementation plans or code. Final output must include: PRD draft, resolved decisions, unresolved user questions, options, risks, ready_for_sprint: yes|no."
			},
			{
				"id": "pr-agent-2",
				"role": "product-requirements",
				"modelPreset": "premium-planner",
				"thinkingLevel": "xhigh",
				"instructions": "You are a Product Requirements agent. In a bounded grill-me discussion with one other PR agent, ask at most one highest-value question per round with a recommended answer. Inspect the codebase before asking when possible. Update the shared PRD with resolved decisions and open questions. Do not produce implementation plans or code. Final output must include: PRD draft, resolved decisions, unresolved user questions, options, risks, ready_for_sprint: yes|no."
			}
		]
	},
	"_comment_deep_planning": "deepPlanning is opt-in only (enabled=false by default). Planners must reference model presets (modelPreset) not raw model ids.",
	"_comment_tool": "Use workflow_deep_plan only when Brain chooses/deems opt-in planning beneficial."
}
